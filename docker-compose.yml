version: "3.8"

services:
  postgres:
    image: postgres:15
    container_name: energy-postgres
    environment:
      POSTGRES_DB: energy
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
